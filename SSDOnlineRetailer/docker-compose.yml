version: '3.4'

services:
  seq:
    image: datalust/seq
    ports: 
        - "5341:5341"
        - "5342:80"
    volumes:
        - rps_seq_data1:/data
    environment:
        - ACCEPT_EULA=Y

  
volumes:
  rps_seq_data1: